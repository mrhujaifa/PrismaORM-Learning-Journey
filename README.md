# Prisma Client Cheat Sheet (বাংলা)

এই ডকুমেন্টে **Prisma Client**-এর সবচেয়ে বেশি ব্যবহৃত মেথড ও
কীওয়ার্ডগুলো **সহজ ভাষায়**, **কখন ব্যবহার করবেন**, আর **ছোট কোড
উদাহরণসহ** গুছিয়ে দেওয়া হলো।

------------------------------------------------------------------------

## 1) Prisma Client কী?

**Prisma Client** হলো আপনার ডাটাবেসের সাথে কথা বলার টাইপ-সেইফ ORM
ক্লায়েন্ট।\
আপনি SQL না লিখেও `findMany`, `create`, `update` ইত্যাদি মেথড দিয়ে কাজ
করতে পারেন।\
আর প্রয়োজন হলে `$queryRaw` / `$executeRaw` দিয়ে **র' SQL**-ও চালাতে
পারেন।

``` ts
import { PrismaClient } from "@prisma/client";
const prisma = new PrismaClient();
```

------------------------------------------------------------------------

## 2) গ্লোবাল / ইউটিলিটি মেথড

  -----------------------------------------------------------------------
  মেথড                    কী করে                  কখন ব্যবহার করবেন
  ----------------------- ----------------------- -----------------------
  `$connect()`            ডাটাবেসে কানেক্ট করে    স্ক্রিপ্ট/ওয়ার্কার
                                                  শুরুতে

  `$disconnect()`         কানেকশন বন্ধ করে        স্ক্রিপ্ট শেষে রিসোর্স
                                                  রিলিজ

  `$on()`                 ইভেন্ট/কুয়েরি লগ শোনে   ডিবাগ/লগিং

  `$transaction()`        একাধিক অপারেশনকে এক     All-or-Nothing দরকার
                          ইউনিটে চালায়            হলে

  `$queryRaw`             র' SQL দিয়ে **রিড**     জটিল কুয়েরি যেখানে ORM
                                                  যথেষ্ট না

  `$executeRaw`           র' SQL দিয়ে **রাইট**    কাস্টম UPDATE/DELETE

  `$extends()`            ক্লায়েন্ট এক্সটেন্ড     কাস্টম মেথড/ফিল্ড যোগ
                                                  করতে
  -----------------------------------------------------------------------

### উদাহরণ

\*\*\$connect / $disconnect** ```ts await prisma.$connect(); // ... কাজ
await prisma.\$disconnect();


    **$on (Logging)**
    ```ts
    prisma.$on("query", (e) => {
      console.log("Query:", e.query, "Time:", e.duration);
    });

\*\*$transaction (All or Nothing)** ```ts await prisma.$transaction(\[
prisma.user.create({ data: { email: "a@test.com" } }),
prisma.profile.create({ data: { bio: "Hello" } }),\]);


    **$queryRaw (Read)**
    ```ts
    const users = await prisma.$queryRaw`SELECT * FROM "User" WHERE age > 18`;

\*\*$executeRaw (Write)** ```ts await prisma.$executeRaw`UPDATE "User" SET active = true WHERE last_login IS NOT NULL`;


    **$extends (Custom method)**
    ```ts
    const xprisma = prisma.$extends({
      model: {
        user: {
          async findActive() {
            return prisma.user.findMany({ where: { active: true } });
          },
        },
      },
    });

    const actives = await xprisma.user.findActive();

------------------------------------------------------------------------

## 3) CRUD মেথড (Model লেভেল)

ধরি আপনার একটি `User` মডেল আছে। তখন `prisma.user` থেকে নিচেরগুলো পাবেন:

  মেথড           কাজ                               কখন
  -------------- --------------------------------- ------------------
  `findMany`     অনেক রেকর্ড আনে                   লিস্ট দেখাতে
  `findUnique`   ইউনিক ফিল্ড দিয়ে ১টা              id/email দিয়ে
  `findFirst`    শর্ত মিললে প্রথমটা                যেকোনো ১টা দরকার
  `create`       নতুন রেকর্ড                       ডাটা ইনসার্ট
  `update`       আপডেট                             id দিয়ে এডিট
  `delete`       ডিলিট                             id দিয়ে রিমুভ
  `upsert`       আছে হলে আপডেট, না থাকলে ক্রিয়েট   Sync টাইপ কাজ
  `count`        কয়টা রেকর্ড                       টোটাল গোনা
  `aggregate`    sum/avg/min/max                   রিপোর্ট
  `groupBy`      গ্রুপ করে রেজাল্ট                 অ্যানালিটিক্স

### উদাহরণ

**findMany**

``` ts
const users = await prisma.user.findMany();
```

**findUnique**

``` ts
const user = await prisma.user.findUnique({
  where: { id: "uuid" },
});
```

**create**

``` ts
const u = await prisma.user.create({
  data: { email: "test@test.com", name: "Test" },
});
```

**update**

``` ts
await prisma.user.update({
  where: { id: "uuid" },
  data: { name: "New Name" },
});
```

**delete**

``` ts
await prisma.user.delete({
  where: { id: "uuid" },
});
```

**upsert**

``` ts
await prisma.user.upsert({
  where: { email: "a@test.com" },
  update: { name: "Updated" },
  create: { email: "a@test.com", name: "Created" },
});
```

**count / aggregate**

``` ts
const total = await prisma.user.count();

const stats = await prisma.user.aggregate({
  _avg: { age: true },
  _max: { age: true },
});
```

**groupBy**

``` ts
const byRole = await prisma.user.groupBy({
  by: ["role"],
  _count: { _all: true },
});
```

------------------------------------------------------------------------

## 4) Query Options / Keywords

  কীওয়ার্ড     কাজ
  ------------ ----------------------
  `where`      ফিল্টার
  `select`     নির্দিষ্ট ফিল্ড নেবে
  `include`    রিলেশন সহ নেবে
  `orderBy`    সাজাবে
  `take`       লিমিট
  `skip`       অফসেট
  `distinct`   ইউনিক রো
  `cursor`     পেজিনেশন

### উদাহরণ

``` ts
const users = await prisma.user.findMany({
  where: { active: true },
  select: { id: true, email: true },
  orderBy: { createdAt: "desc" },
  take: 10,
  skip: 20,
});
```

**include (Relation)**

``` ts
const posts = await prisma.user.findMany({
  include: { posts: true },
});
```

------------------------------------------------------------------------

## 5) Relation Helper Keywords

  কীওয়ার্ড       কাজ
  -------------- -------------------
  `connect`      আগের রেকর্ড লিঙ্ক
  `disconnect`   লিঙ্ক ছাড়ানো
  `create`       সাথে নতুন বানানো
  `set`          পুরো রিলেশন সেট

``` ts
await prisma.post.update({
  where: { id: "p1" },
  data: {
    author: {
      connect: { id: "u1" },
    },
  },
});
```

------------------------------------------------------------------------

## 6) কখন কোনটা ব্যবহার করবেন (শর্ট গাইড)

-   **সাধারণ CRUD** → `findMany / create / update / delete`
-   **একাধিক অপারেশন একসাথে সেফলি** → `$transaction`
-   **জটিল SQL রিড** → `$queryRaw`
-   **কাস্টম SQL আপডেট/ডিলিট** → `$executeRaw`
-   **পারফরম্যান্স/ডিবাগ** → `$on`
-   **নিজের হেল্পার মেথড** → `$extends`
-   **পেজিনেশন/ফিল্টার** → `where / take / skip / orderBy`

------------------------------------------------------------------------

## 7) সংক্ষেপে

-   এগুলোকে বলা হয় **Prisma Client Methods & Query API**\
-   এগুলোর কাজ: **ডাটাবেসে টাইপ-সেইফ ভাবে CRUD, ট্রানজ্যাকশন, র' SQL,
    রিলেশন ম্যানেজমেন্ট**\
-   আপনি কাজের ধরন অনুযায়ী মেথড বেছে নেবেন।

------------------------------------------------------------------------

**Happy Coding with Prisma! 🚀**
